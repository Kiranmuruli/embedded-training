#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("enter the number of a %d\n", a);
    scanf("%d",&a);
    if (a>0)
    printf(" the number is positive a %d\n",a);
    else
    printf("the number is negetive a %d\n",a);
    else

    return 0;
}
