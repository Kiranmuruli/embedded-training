#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("enter no a\n");
           scanf("%d",&a);
           if(a%2==0)
           {
        printf("no is even");
           }
           if(a%2!=0)
            {
            printf("no is odd");
           }
    return 0;
}
